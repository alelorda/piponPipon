@model IEnumerable<Pipo_Pipon.Models.OrdenDetalle>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script></script>

<center>
    <h2><b>Clientes</b></h2>

    <p>
        @Html.ActionLink("Nuevo Cliente", "Create")
    </p>
</center>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreCliente)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ordenDetalles }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ordenDetalles }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ordenDetalles })
        </td>
    </tr>
}

</table>
